[tool.poetry]
name = "projet"
version = "0.1.0"
description = ""
authors = ["Arthur Dias <a.dias@cyberdian.fr>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
pygame = "^2.5.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
